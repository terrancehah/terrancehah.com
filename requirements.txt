# Installed from api/requirements.txt so the root file does not contain
# the word "fastapi". Vercel CLI 59 treats a root FastAPI dependency as a
# single-app framework and zips the whole static site (~240MB) into one
# function, which exceeds the 225MB limit.
-r api/requirements.txt
